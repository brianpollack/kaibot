# Tech Debt TODO

_Generated by KaiBot tech debt scan._

## High Priority 🔴

- [ ] **[Security]** `bashSecurityHook` only checks the first word of a command — piped commands (`ls | evil_binary`), subshells (`$(dangerous)`), and chaining (`ls; rm -rf /`) bypass the allowlist entirely. Parse the full pipeline and validate each segment. — `src/security.ts:58`
- [ ] **[Security]** `git add -A` in `promptAndCommit` stages everything including `.env`, credentials, and secrets. Should selectively stage only agent-modified files or at minimum exclude sensitive patterns. — `src/commit.ts:125`
- [ ] **[Security]** `NpmCommandRunner.start()` spawns arbitrary npm scripts with `shell: true` and no validation. A malicious `package.json` script could execute anything. Consider sandboxing or at least logging what's being run. — `src/web/NpmCommandRunner.ts:107-109`
- [ ] **[Security]** OpenRouter provider mutates `process.env` globals (ANTHROPIC_API_KEY, ANTHROPIC_BASE_URL, ANTHROPIC_AUTH_TOKEN) in the KaiClient constructor. Not safe for concurrent operations and could leak credentials between requests. — `src/KaiClient.ts:152-161`
- [ ] **[Security]** `loadPromptContent` path traversal guard strips `..` and slashes with regex but doesn't use `resolve()` + prefix check like `serveStatic` does. Encoded or creative filenames could potentially escape. — `src/codeAssist.ts:61`
- [ ] **[Security]** `readBody` in `routes.ts` has no size limit — a client could send an arbitrarily large request body causing OOM. Add a `MAX_BODY_SIZE` check. — `src/web/routes.ts:858-865`
- [ ] **[Refactoring]** `handleFeatureFile` is ~145 lines with deeply nested try/catch, multiple file reads, title generation, commit prompting, follow-up session, and logging all interleaved. Extract into smaller methods (e.g. `runAgentPhase`, `commitPhase`, `recordPhase`). — `src/KaiBot.ts:331-476`
- [ ] **[Refactoring]** `handleLinearIssue` is ~120 lines and largely duplicates `handleFeatureFile` structure (title generation, agent processing, commit, record, cleanup). Extract shared pipeline logic into a common method. — `src/KaiBot.ts:172-287`
- [ ] **[Code Quality]** `tryCreateLinearIssue` hardcodes team ID as `"KAIBOT"` instead of reading from env config — this will fail for any team not named "KAIBOT". — `src/feature_creator.ts:517`
- [ ] **[Code Quality]** `KaiBot.handleLinearIssue` and `handleFeatureFile` call `process.exit(1)` on auth errors from deep inside the class — this bypasses cleanup, unmounting, and `finally` blocks; should throw and let the CLI entry point handle exit. — `src/KaiBot.ts:274,455`

## Medium Priority 🟡

- [ ] **[Refactoring]** `routes.ts` `handleRequest` is a 580+ line function with a giant if/else chain for every route. Extract route handlers into separate functions or a simple router map. — `src/web/routes.ts:124-709`
- [ ] **[Duplicate Code]** `expandTilde` is duplicated identically in `kai_bot.ts:104-110` and `routes.ts:97-103`. Extract to a shared utility (e.g. `src/paths.ts`). — `src/kai_bot.ts:104`, `src/web/routes.ts:97`
- [ ] **[Duplicate Code]** Model/provider resolution logic (`provider === "openrouter" ? getOpenRouterModel() : process.env.KAI_MODEL ?? ...`) is repeated 4 times across `kai_bot.ts`. Should be a single helper function. — `src/kai_bot.ts:208-211,267-271,306-309`
- [ ] **[Duplicate Code]** Agent message streaming loop (iterate messages, duck-type content blocks for "text" and "tool_use", route to uiStore) is repeated nearly identically in `KaiAgent.ts:102-134`, `codeAssist.ts:95-108`, `todoAssist.ts:87-101`, and `followupSession.ts:64-87`. Extract a shared `streamAgentMessages` utility.
- [ ] **[Duplicate Code]** File dedup loop (check `existsSync`, bump `_2`, `_3` suffix) is copy-pasted in both `writeFeatureFromDescription` (lines 233-238) and `reviewAndWriteFeature` (lines 339-346). Extract a shared `resolveUniqueFeaturePath()` helper. — `src/feature_creator.ts`
- [ ] **[Duplicate Code]** `readPlanSection` in `KaiAgent.ts:391-412` and `getSectionLines` in `linear.ts:266-280` both extract markdown sections between `##` headings with nearly identical logic. Extract a shared `extractMarkdownSection()` utility.
- [ ] **[Code Quality]** `spendTracker.ts` reads and parses the entire `features.json` from disk on every WebSocket state broadcast (every UI change event) via `getWebState()`. Should cache with TTL or file-watcher invalidation. — `src/web/spendTracker.ts:16-39`, `src/web/wsHandler.ts:63`
- [ ] **[Code Quality]** Multiple files silently swallow errors with empty `catch {}` blocks (~8 in KaiBot.ts, ~12 in routes.ts). At minimum add debug-level logging so failures aren't invisible. — project-wide
- [ ] **[Code Quality]** `env.ts` `parseDotEnv` doesn't handle escaped characters, multi-line values, `export` prefix, or inline comments — consider documenting these limitations or using a well-tested dotenv library. — `src/env.ts:20-44`
- [ ] **[Code Cleanup]** `models.ts` imports `https` from `node:https` (line 142) but `fetchModels` uses the global `fetch` API. The `https` import is only used by `fetchOpenRouterModels` — inconsistent approach for the same task across two similar functions. — `src/models.ts:142,152,186`
- [ ] **[Code Cleanup]** `linear.ts` imports `LinearFetch` and `User` from `@linear/sdk` but `getCurrentUser()` is never called anywhere in the codebase. Dead import and dead method. — `src/linear.ts:16,57-59`
- [ ] **[Code Cleanup]** `index.ts` is a comment-only file with no exports — either export the public API or remove the file. — `src/index.ts`
- [ ] **[Refactoring]** Default model string `"claude-opus-4-6"` is repeated in at least 4 places (`KaiBot.ts:66`, `kai_bot.ts:80,211,289`). Use the single `DEFAULT_MODEL` export from `models.ts` everywhere. — multiple files
- [ ] **[Refactoring]** `SUMMARY_MODEL` and `TITLE_MODEL` are both hardcoded to `"claude-haiku-4-5-20251001"` as separate constants. Unify into a single `UTILITY_MODEL` constant. — `src/KaiAgent.ts:324,356`

## Low Priority

- [ ] **[Testing]** No test coverage for `KaiClient` (constructor validation, `buildOptions`, `findClaudeExecutable`, env mutation for OpenRouter, `restoreEnv`). — `src/KaiClient.ts`
- [ ] **[Testing]** No test coverage for `codeAssist.ts` (loading options, prompt interpolation, path traversal prevention in `loadPromptContent`). — `src/codeAssist.ts`
- [ ] **[Testing]** No test coverage for `todoAssist.ts` (`loadTodoItems`, `todoExists`, `runTodoPlan`). — `src/todoAssist.ts`
- [ ] **[Testing]** No test coverage for `commit.ts` (`extractFeatureDescription`, `buildCommitMessage`, `promptAndCommit` behavior). — `src/commit.ts`
- [ ] **[Testing]** No test coverage for `web/hmac.ts` (`verifyRequestHmac`, `verifyWsHmac` — timing attacks, signature verification, replay window edge cases). — `src/web/hmac.ts`
- [ ] **[Testing]** No test coverage for `web/NpmCommandRunner.ts` (start/stop/restart, output buffering, MAX_BUFFER enforcement, package.json watching). — `src/web/NpmCommandRunner.ts`
- [ ] **[Testing]** No test coverage for `web/followupSession.ts` (session lifecycle, follow-up streaming, log persistence). — `src/web/followupSession.ts`
- [ ] **[Testing]** No test coverage for `web/spendTracker.ts` (`getTodaySpend` with various date/record combinations). — `src/web/spendTracker.ts`
- [ ] **[Testing]** No test coverage for `env.ts` (`parseDotEnv` edge cases — multiline values, escaped quotes, comments, duplicate keys). — `src/env.ts`
- [ ] **[Testing]** No test coverage for `git.ts` (`getGitBranch`, `getLastCommitHash`). — `src/git.ts`
- [ ] **[Testing]** No test coverage for `changelog.ts` (`extractDescription`, `appendChangelog`, `formatDate`). — `src/changelog.ts`
- [ ] **[Performance]** `refreshPlanLines` reads the entire feature file from disk on every message from the agent stream. Should debounce or only read when tool_result messages indicate the file was modified. — `src/KaiAgent.ts:137`
- [ ] **[Performance]** `KaiBot.checkForNewFeatures` reads feature file content with `readFileSync` on every poll cycle (every 2s) for every `.md` file, even when already seen. — `src/KaiBot.ts:315`
- [ ] **[Performance]** `getFeaturesList` in `routes.ts` reads every file in features/, hold/, and log/ directories synchronously on each API call. Should cache with file-watcher invalidation. — `src/web/routes.ts:760-835`
- [ ] **[Dependencies]** `@types/node` is pinned to `^25.4.0` which is a very recent major — verify compatibility with the Node 20 LTS target declared in `engines`. — `package.json`
