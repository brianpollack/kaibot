# Tech Debt TODO

_Generated by KaiBot tech debt scan._

## High Priority

- [ ] **[Security]** `bashSecurityHook` only checks the first word of the command — piped commands (`ls | rm -rf /`), subshells (`$(dangerous_cmd)`), and command chaining (`ls; rm -rf /`) bypass the allowlist entirely — `src/security.ts:53`
- [ ] **[Security]** `git add -A` in `promptAndCommit` stages **all** files including `.env`, secrets, and unrelated changes — should stage only agent-modified files or at minimum exclude sensitive patterns — `src/commit.ts:115`
- [ ] **[Security]** `ALLOWED_COMMANDS` includes `rm`, `chmod`, `curl`, `wget`, `sed`, and `awk` which can be destructive or exfiltrate data; consider restricting flags (e.g. disallow `rm -rf /`) or tightening the allowlist — `src/security.ts:10-43`
- [ ] **[Refactoring]** `feature_creator.ts` has heavy duplication: the file-dedup loop (check `existsSync`, bump suffix) is copy-pasted in both `writeFeatureFromDescription` (lines 233-238) and `reviewAndWriteFeature` (lines 339-346) — extract a shared `resolveUniqueFeaturePath()` helper — `src/feature_creator.ts`
- [ ] **[Code Quality]** `tryCreateLinearIssue` hardcodes team ID as `"KAIBOT"` instead of reading the resolved team ID from env config — this will fail for any team not named "KAIBOT" — `src/feature_creator.ts:517`
- [ ] **[Code Quality]** `KaiBot.handleLinearIssue` calls `process.exit(1)` on auth errors from deep inside the class — this bypasses cleanup, unmounting, and `finally` blocks; should throw and let the CLI entry point handle exit — `src/KaiBot.ts:193,279`

## Medium Priority

- [ ] **[Code Cleanup]** `linear.ts` contains blocks of commented-out debug code: commented `unassigned` query (lines 65-71), commented `console.log` calls (lines 76-78, 82) — remove or convert to a proper debug/logging utility — `src/linear.ts:65-82`
- [ ] **[Code Cleanup]** `linear.ts:88` has a stray `console.log` in production code (`"has no issues"`) — should use a logger or be removed — `src/linear.ts:88`
- [ ] **[Refactoring]** `extractDescription` in `changelog.ts` and `extractFeatureDescription` in `commit.ts` both parse markdown sections from feature file content with overlapping logic — extract a shared `parseFeatureFileContent()` utility — `src/changelog.ts:73`, `src/commit.ts:46`
- [ ] **[Code Quality]** `KaiBot.handleFeatureFile` and `handleLinearIssue` both have nearly identical error-handling blocks (check `isAuthError`, set status, reset to watching) — extract a shared error-handling method — `src/KaiBot.ts:186-206,272-288`
- [ ] **[Refactoring]** `App.tsx` is ~850 lines with 10+ components in a single file — split into separate component files under `src/ui/components/` (Header, ThinkingPanel, CommandPanel, PlanPanel, CommitPrompt, HotkeyBar, FeatureInput, ModelSelector) — `src/ui/App.tsx`
- [ ] **[Code Quality]** `env.ts` `parseDotEnv` doesn't handle escaped characters, multi-line values, `export` prefix, or inline comments — consider using a well-tested dotenv library or documenting these limitations — `src/env.ts:20-44`
- [ ] **[Documentation]** `slugify` is exported from both `src/slugify.ts` and re-exported/imported in `src/feature_creator.ts` — the standalone `src/slugify.ts` exists but `feature_creator.ts` also has `deriveFeatureName` that does slug-adjacent work; clarify which is canonical — `src/slugify.ts`, `src/feature_creator.ts`
- [ ] **[Code Quality]** `FileOpsPanel` accepts a `cols` prop but aliases it to unused `_cols`, then hardcodes `pathWidth = 120` and `previewWidth = 80` — should compute widths from the actual `cols` value — `src/ui/App.tsx:221-233`
- [ ] **[Refactoring]** Default model string `"claude-opus-4-6"` is repeated in 4 places — `KaiBot.ts:51`, `kai_bot.ts:34`, `kai_bot.ts:78`, `models.ts:21` — use the single `DEFAULT_MODEL` export from `models.ts` everywhere — `src/kai_bot.ts`, `src/KaiBot.ts`

## Low Priority

- [ ] **[Duplicate Code]** `readPlanSection` in `KaiAgent.ts` (lines 244-265) and `getSectionLines` in `linear.ts` (lines 253-267) both extract markdown sections between `##` headings — extract a shared `extractMarkdownSection(content, heading)` utility — `src/KaiAgent.ts`, `src/linear.ts`
- [ ] **[Duplicate Code]** `isGitRepo` check (call `git rev-parse --is-inside-work-tree`) is implemented separately in `commit.ts:12-23` and `KaiBot.ts:323-325` — extract to a shared `git.ts` utility — `src/commit.ts`, `src/KaiBot.ts`
- [ ] **[Code Quality]** `getNextReadyIssue` fetches **all** assigned issues then filters in-memory — should push the state-type filter into the Linear API query to reduce payload size — `src/linear.ts:61-94`
- [ ] **[Code Cleanup]** `index.ts` is just a comment with no exports — either export the public API or remove the file — `src/index.ts`
- [ ] **[Code Cleanup]** `src/__tests__/placeholder.test.ts` is an empty placeholder test file — remove it — `src/__tests__/placeholder.test.ts`
- [ ] **[Documentation]** `KaiAgent.processFeature` streams messages and updates UI but the interaction between stage transitions (reading → thinking → planning → executing) is undocumented and relies on heuristic detection (checking for `## Plan` text, first Edit/Write) — add inline docs or a state diagram — `src/KaiAgent.ts:76-110`
- [ ] **[Performance]** `refreshPlanLines` reads the entire feature file from disk on **every** message from the agent stream — could debounce or only read when tool_result messages indicate the file was modified — `src/KaiAgent.ts:114,232-242`
- [ ] **[Code Quality]** `UIStore` is a singleton with mutable state and no way to reset for testing — makes unit testing UI-dependent code difficult; consider a factory function or dependency injection — `src/ui/store.ts:106-377`
- [ ] **[Dependencies]** `@types/node` is pinned to `^25.4.0` which is a very recent major — verify this matches the Node 20 LTS target declared in `engines` — `package.json:33`
- [ ] **[Code Quality]** `FeatureInput` component's `blankCount` is tracked via `useRef` which doesn't trigger re-renders — the `(1/3 blank lines entered)` hint at line 588 reads from `blankCount.current` but may show stale values since `useRef` changes don't cause re-render — `src/ui/App.tsx:483-593`
